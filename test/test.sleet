#! drizzle

view
    input.input(bind:value=name)
    h3 hello $html(name | echo('a' true 1 name)) !
    | $html(name)

script..
export default {
    helpers: {
        echo (data) {
            console.log(data)
            return data
        }
    }
}
